/**
*					--DAO-Generator--
*
* Java code generated by the DAO-Generator developed by akcess(www.akcess.in)
* Date of code generation: Sun Jun 27 03:16:04 ART 2010
*
* DAO-Generator Version 1.2
*
*/

package metalsoft.datos.dao;
import java.math.*;
import java.sql.*;
import java.net.URL;
import java.util.*;
import metalsoft.datos.exception.*;
import metalsoft.datos.dbobject.*;
import metalsoft.datos.idao.*;

/**
* 
* Implementation of ProductoRecordCountDAO interface 
* 
*/

public class ProductoRecordCountDAOImpl implements ProductoRecordCountDAO
{
	public ProductoRecordCountDAOImpl()
	{
	}
	public ProductoRecordCount getRecordCount(Connection con) throws ProductoRecordCountException
	{
		PreparedStatement stmt = null;
		ResultSet rs = null;
		try {
			stmt = con.prepareStatement("SELECT COUNT(*) FROM PRODUCTO");
			rs = stmt.executeQuery();
			rs.next();
 			ProductoRecordCount vo = new ProductoRecordCount();
			vo.setCount(rs.getInt(1));
			return vo;
		}catch (SQLException sqle) {
			throw new ProductoRecordCountException(sqle);
		}
		catch (Exception e) {
			throw new ProductoRecordCountException(e);
		}
	}



	public ProductoRecordCount getRecordCount(Connection con, String whereClause) throws ProductoRecordCountException
	{
		PreparedStatement stmt = null;
		ResultSet rs = null;
		try {
			stmt = con.prepareStatement("SELECT COUNT(*) FROM PRODUCTO  " + whereClause);
			rs = stmt.executeQuery();
			rs.next();
 			ProductoRecordCount vo = new ProductoRecordCount();
			vo.setCount(rs.getInt(1));
			return vo;
		}catch (SQLException sqle) {
			throw new ProductoRecordCountException(sqle);
		}
		catch (Exception e) {
			throw new ProductoRecordCountException(e);
		}
	}
}
