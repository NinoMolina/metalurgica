/**
*					--DAO-Generator--
*
* Java code generated by the DAO-Generator developed by akcess(www.akcess.in)
* Date of code generation: Sun Jun 27 03:16:04 ART 2010
*
* DAO-Generator Version 1.2
*
*/

package metalsoft.datos.dao;
import java.math.*;
import java.sql.*;
import java.net.URL;
import java.util.*;
import metalsoft.datos.exception.*;
import metalsoft.datos.dbobject.*;
import metalsoft.datos.idao.*;


/**
* 
* Implementation of ProvinciaDAO interface 
* 
*/


public class ProvinciaDAOImpl implements ProvinciaDAO
{


/**
* Method deletes a record from table PROVINCIA
* @param ProvinciaPK provinciapk
* @param  Connection  con
* @return  int
*
*/


	public int delete(ProvinciaPKDB provinciapk , Connection con)throws ProvinciaException{
		PreparedStatement ps = null;
		try {
			ps = con.prepareStatement("delete from  PROVINCIA where idprovincia = ?");
			ps.setLong(1, provinciapk.getIdprovincia());
			return(ps.executeUpdate());
		}catch(SQLException sqle) {throw new ProvinciaException(sqle);}
		catch(Exception e) {throw new ProvinciaException(e);}
	}



/**
* This method updates a record in table PROVINCIA
* @param ProvinciaPK
* @param Provincia
* @param  Connection con
* @return   int
*/

	public int update(ProvinciaPKDB provinciapk, ProvinciaDB provincia, Connection con)throws ProvinciaException{
		PreparedStatement ps = null;
		try
		{
			ps = con.prepareStatement("update PROVINCIA set NOMBRE = ?  where idprovincia = ?");
				ps.setString(1,provincia.getNombre());
				ps.setLong(2,provinciapk.getIdprovincia());

				return(ps.executeUpdate());
		}catch(SQLException sqle){throw new ProvinciaException(sqle);}
		catch(Exception e){throw new ProvinciaException(e);}
	}

/**
* This method inserts data in table PROVINCIA
*
* @param Provincia provincia
* @param   Connection con
* @return  ProvinciaPK
*/

	public int insert(ProvinciaDB provincia ,Connection con)throws ProvinciaException {

		PreparedStatement ps = null;
		try
		{
			ps = con.prepareStatement("insert into PROVINCIA( NOMBRE) values (?)");
				ps.setString(1,provincia.getNombre());

				return(ps.executeUpdate());
		}catch(SQLException sqle){throw new ProvinciaException(sqle);}
		catch(Exception e){throw new ProvinciaException(e);}
	}

/**
* 
* Returns a row from the provincia table for the primary key passed as parameter.
* 
*/

	public ProvinciaDB findByPrimaryKey(long idprovincia, Connection con) throws ProvinciaException{
		PreparedStatement stmt = null;
		ResultSet rs = null;
		try {
	  		final String SQLSTATEMENT = "Select idprovincia, nombre from provincia where idprovincia = ?";
	  		stmt=con.prepareStatement(SQLSTATEMENT);
	  		stmt.setLong(1, idprovincia);
	  		rs = stmt.executeQuery();
	  		return fetchSingleResult(rs);
	  	}
	  	catch (SQLException sqle) {
	  		throw new ProvinciaException(sqle);
	  	}
	    catch(Exception e){throw new ProvinciaException(e);}
	  	 finally {}
	}

/**
* 
* Returns a row from the provincia table for the primary key object passed as parameter.
* 
* @param  ProvinciaPK provinciapk
* @param Connection con
* @return  Provincia
*/

	public ProvinciaDB findByPrimaryKey(ProvinciaPKDB provinciapk, Connection con) throws ProvinciaException{
		return findByPrimaryKey(provinciapk.getIdprovincia(), con);
	}

/**
*
* Returns all rows from provincia table where IDPROVINCIA= idprovincia
*
* @param   long  idprovincia
* @param   Connection con
* @return  Provincia[]
*/

	public ProvinciaDB[] findByIdprovincia(long idprovincia, Connection con) throws ProvinciaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idprovincia, nombre from provincia where idprovincia = ? order by idprovincia";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setLong( 1, idprovincia );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new ProvinciaException(sqle);
			}
			catch(Exception e){
					throw new ProvinciaException(e);
			}
			finally{}
	}

/**
*
* Returns all rows from provincia table where NOMBRE= nombre
*
* @param   String  nombre
* @param   Connection con
* @return  Provincia[]
*/

	public ProvinciaDB[] findByNombre(String nombre, Connection con) throws ProvinciaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idprovincia, nombre from provincia where nombre = ? order by nombre";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setString( 1, nombre );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new ProvinciaException(sqle);
			}
			catch(Exception e){
					throw new ProvinciaException(e);
			}
			finally{}
	}

/**
* Returns all rows from provincia table 
*
* @param Connection con
* @return  Provincia[]
*
*/

	public ProvinciaDB[] findAll( Connection con) throws ProvinciaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idprovincia, nombre from provincia";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new ProvinciaException(sqle);
			}
			catch(Exception e){
					throw new ProvinciaException(e);
			}
			finally{}
	}

/**
* Returns rows from provincia table by executing the passed sql statement
* after setting the passed values in Object[]
*
* @param String selectStatement
* @param Object[] sqlParams
* @param Connection con
* @return  Provincia[]
*
*/

	public ProvinciaDB[] findExecutingUserSelect(String selectStatement, Object[] sqlParams, Connection con) throws ProvinciaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			final String SQL_STATEMENT = selectStatement;
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					for (int i=0; i<sqlParams.length; i++ ) {
							stmt.setObject( i+1, sqlParams[i] );
					}
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new ProvinciaException(sqle);
			}
			catch(Exception e){
					throw new ProvinciaException(e);
			}
			finally{}
	}

/**
* Returns rows from provincia table by executing the select all fields statement
* after setting the passed where clause and values in Object[]
*
* @param String whereClause
* @param Object[] sqlParams
* @param Connection con
* @return  Provincia[]
*
*/

	public ProvinciaDB[] findExecutingUserWhere(String whereClause, Object[] sqlParams, Connection con) throws ProvinciaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_SELECT ="Select idprovincia, nombre from provincia";
			final String SQL_STATEMENT =SQL_SELECT + " where " + whereClause;
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					for (int i=0; i<sqlParams.length; i++ ) {
							stmt.setObject( i+1, sqlParams[i] );
					}
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new ProvinciaException(sqle);
			}
			catch(Exception e){
					throw new ProvinciaException(e);
			}
			finally{}
	}

/**
*
* Populates a Data Transfer Object by fetching single record from resultSet 
*
* @param ResultSet rs
* @return  Provincia
*
*/

	protected ProvinciaDB fetchSingleResult(ResultSet rs) throws SQLException
	{
			if (rs.next()) {
					ProvinciaDB dto = new ProvinciaDB();
					populateVO( dto, rs);
				return dto;
			} else {
				return null;
			}
	}

/**
* 
* Populates a Data Transfer Object by fetching data from  ResultSet
* 
* @param Provincia dto
* @param   ResultSet rs
* @return  void
*/

	protected void populateVO(ProvinciaDB dto, ResultSet rs) throws SQLException
	{
		 dto.setIdprovincia(rs.getLong("idprovincia"));
		 dto.setNombre(rs.getString("nombre"));
	}

/**
* 
* Returns an array of Value Objects by fetching data from resultSet
* 
* @param   ResultSet rs
* @return  Provincia[]
*/

	protected ProvinciaDB[]  fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			ProvinciaDB dto = new ProvinciaDB();
			populateVO( dto, rs);
			resultList.add(dto);
		}
		ProvinciaDB ret[] = new ProvinciaDB[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}
}
