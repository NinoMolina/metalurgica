/**
*					--DAO-Generator--
*
* Java code generated by the DAO-Generator developed by akcess(www.akcess.in)
* Date of code generation: Sun Jun 27 03:16:05 ART 2010
*
* DAO-Generator Version 1.2
*
*/

package metalsoft.datos.dao;
import java.math.*;
import java.sql.*;
import java.net.URL;
import java.util.*;
import metalsoft.datos.exception.*;
import metalsoft.datos.dbobject.*;
import metalsoft.datos.idao.*;


/**
* 
* Implementation of RoturaDAO interface 
* 
*/


public class RoturaDAOImpl implements RoturaDAO
{


/**
* Method deletes a record from table ROTURA
* @param RoturaPK roturapk
* @param  Connection  con
* @return  int
*
*/


	public int delete(RoturaPKDB roturapk , Connection con)throws RoturaException{
		PreparedStatement ps = null;
		try {
			ps = con.prepareStatement("delete from  ROTURA where idrotura = ?");
			ps.setLong(1, roturapk.getIdrotura());
			return(ps.executeUpdate());
		}catch(SQLException sqle) {throw new RoturaException(sqle);}
		catch(Exception e) {throw new RoturaException(e);}
	}



/**
* This method updates a record in table ROTURA
* @param RoturaPK
* @param Rotura
* @param  Connection con
* @return   int
*/

	public int update(RoturaPKDB roturapk, RoturaDB rotura, Connection con)throws RoturaException{
		PreparedStatement ps = null;
		try
		{
			ps = con.prepareStatement("update ROTURA set NOMBRE = ? , DESCRIPCION = ?  where idrotura = ?");
				ps.setString(1,rotura.getNombre());
				ps.setString(2,rotura.getDescripcion());
				ps.setLong(3,roturapk.getIdrotura());

				return(ps.executeUpdate());
		}catch(SQLException sqle){throw new RoturaException(sqle);}
		catch(Exception e){throw new RoturaException(e);}
	}

/**
* This method inserts data in table ROTURA
*
* @param Rotura rotura
* @param   Connection con
* @return  RoturaPK
*/

	public int insert(RoturaDB rotura ,Connection con)throws RoturaException {

		PreparedStatement ps = null;
		try
		{
			ps = con.prepareStatement("insert into ROTURA( NOMBRE, DESCRIPCION) values (?, ?)");
				ps.setString(1,rotura.getNombre());
				ps.setString(2,rotura.getDescripcion());

				return(ps.executeUpdate());
		}catch(SQLException sqle){throw new RoturaException(sqle);}
		catch(Exception e){throw new RoturaException(e);}
	}

/**
* 
* Returns a row from the rotura table for the primary key passed as parameter.
* 
*/

	public RoturaDB findByPrimaryKey(long idrotura, Connection con) throws RoturaException{
		PreparedStatement stmt = null;
		ResultSet rs = null;
		try {
	  		final String SQLSTATEMENT = "Select idrotura, nombre, descripcion from rotura where idrotura = ?";
	  		stmt=con.prepareStatement(SQLSTATEMENT);
	  		stmt.setLong(1, idrotura);
	  		rs = stmt.executeQuery();
	  		return fetchSingleResult(rs);
	  	}
	  	catch (SQLException sqle) {
	  		throw new RoturaException(sqle);
	  	}
	    catch(Exception e){throw new RoturaException(e);}
	  	 finally {}
	}

/**
* 
* Returns a row from the rotura table for the primary key object passed as parameter.
* 
* @param  RoturaPK roturapk
* @param Connection con
* @return  Rotura
*/

	public RoturaDB findByPrimaryKey(RoturaPKDB roturapk, Connection con) throws RoturaException{
		return findByPrimaryKey(roturapk.getIdrotura(), con);
	}

/**
*
* Returns all rows from rotura table where IDROTURA= idrotura
*
* @param   long  idrotura
* @param   Connection con
* @return  Rotura[]
*/

	public RoturaDB[] findByIdrotura(long idrotura, Connection con) throws RoturaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idrotura, nombre, descripcion from rotura where idrotura = ? order by idrotura";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setLong( 1, idrotura );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new RoturaException(sqle);
			}
			catch(Exception e){
					throw new RoturaException(e);
			}
			finally{}
	}

/**
*
* Returns all rows from rotura table where NOMBRE= nombre
*
* @param   String  nombre
* @param   Connection con
* @return  Rotura[]
*/

	public RoturaDB[] findByNombre(String nombre, Connection con) throws RoturaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idrotura, nombre, descripcion from rotura where nombre = ? order by nombre";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setString( 1, nombre );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new RoturaException(sqle);
			}
			catch(Exception e){
					throw new RoturaException(e);
			}
			finally{}
	}

/**
*
* Returns all rows from rotura table where DESCRIPCION= descripcion
*
* @param   String  descripcion
* @param   Connection con
* @return  Rotura[]
*/

	public RoturaDB[] findByDescripcion(String descripcion, Connection con) throws RoturaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idrotura, nombre, descripcion from rotura where descripcion = ? order by descripcion";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setString( 1, descripcion );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new RoturaException(sqle);
			}
			catch(Exception e){
					throw new RoturaException(e);
			}
			finally{}
	}

/**
* Returns all rows from rotura table 
*
* @param Connection con
* @return  Rotura[]
*
*/

	public RoturaDB[] findAll( Connection con) throws RoturaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idrotura, nombre, descripcion from rotura";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new RoturaException(sqle);
			}
			catch(Exception e){
					throw new RoturaException(e);
			}
			finally{}
	}

/**
* Returns rows from rotura table by executing the passed sql statement
* after setting the passed values in Object[]
*
* @param String selectStatement
* @param Object[] sqlParams
* @param Connection con
* @return  Rotura[]
*
*/

	public RoturaDB[] findExecutingUserSelect(String selectStatement, Object[] sqlParams, Connection con) throws RoturaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			final String SQL_STATEMENT = selectStatement;
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					for (int i=0; i<sqlParams.length; i++ ) {
							stmt.setObject( i+1, sqlParams[i] );
					}
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new RoturaException(sqle);
			}
			catch(Exception e){
					throw new RoturaException(e);
			}
			finally{}
	}

/**
* Returns rows from rotura table by executing the select all fields statement
* after setting the passed where clause and values in Object[]
*
* @param String whereClause
* @param Object[] sqlParams
* @param Connection con
* @return  Rotura[]
*
*/

	public RoturaDB[] findExecutingUserWhere(String whereClause, Object[] sqlParams, Connection con) throws RoturaException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_SELECT ="Select idrotura, nombre, descripcion from rotura";
			final String SQL_STATEMENT =SQL_SELECT + " where " + whereClause;
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					for (int i=0; i<sqlParams.length; i++ ) {
							stmt.setObject( i+1, sqlParams[i] );
					}
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new RoturaException(sqle);
			}
			catch(Exception e){
					throw new RoturaException(e);
			}
			finally{}
	}

/**
*
* Populates a Data Transfer Object by fetching single record from resultSet 
*
* @param ResultSet rs
* @return  Rotura
*
*/

	protected RoturaDB fetchSingleResult(ResultSet rs) throws SQLException
	{
			if (rs.next()) {
					RoturaDB dto = new RoturaDB();
					populateVO( dto, rs);
				return dto;
			} else {
				return null;
			}
	}

/**
* 
* Populates a Data Transfer Object by fetching data from  ResultSet
* 
* @param Rotura dto
* @param   ResultSet rs
* @return  void
*/

	protected void populateVO(RoturaDB dto, ResultSet rs) throws SQLException
	{
		 dto.setIdrotura(rs.getLong("idrotura"));
		 dto.setNombre(rs.getString("nombre"));
		 dto.setDescripcion(rs.getString("descripcion"));
	}

/**
* 
* Returns an array of Value Objects by fetching data from resultSet
* 
* @param   ResultSet rs
* @return  Rotura[]
*/

	protected RoturaDB[]  fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			RoturaDB dto = new RoturaDB();
			populateVO( dto, rs);
			resultList.add(dto);
		}
		RoturaDB ret[] = new RoturaDB[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}
}
