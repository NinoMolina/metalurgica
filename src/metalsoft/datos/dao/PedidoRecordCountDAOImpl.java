/**
*					--DAO-Generator--
*
* Java code generated by the DAO-Generator developed by akcess(www.akcess.in)
* Date of code generation: Sun Jun 27 03:16:03 ART 2010
*
* DAO-Generator Version 1.2
*
*/

package metalsoft.datos.dao;
import java.math.*;
import java.sql.*;
import java.net.URL;
import java.util.*;
import metalsoft.datos.exception.*;
import metalsoft.datos.dbobject.*;
import metalsoft.datos.idao.*;

/**
* 
* Implementation of PedidoRecordCountDAO interface 
* 
*/

public class PedidoRecordCountDAOImpl implements PedidoRecordCountDAO
{
	public PedidoRecordCountDAOImpl()
	{
	}
	public PedidoRecordCount getRecordCount(Connection con) throws PedidoRecordCountException
	{
		PreparedStatement stmt = null;
		ResultSet rs = null;
		try {
			stmt = con.prepareStatement("SELECT COUNT(*) FROM PEDIDO");
			rs = stmt.executeQuery();
			rs.next();
 			PedidoRecordCount vo = new PedidoRecordCount();
			vo.setCount(rs.getInt(1));
			return vo;
		}catch (SQLException sqle) {
			throw new PedidoRecordCountException(sqle);
		}
		catch (Exception e) {
			throw new PedidoRecordCountException(e);
		}
	}



	public PedidoRecordCount getRecordCount(Connection con, String whereClause) throws PedidoRecordCountException
	{
		PreparedStatement stmt = null;
		ResultSet rs = null;
		try {
			stmt = con.prepareStatement("SELECT COUNT(*) FROM PEDIDO  " + whereClause);
			rs = stmt.executeQuery();
			rs.next();
 			PedidoRecordCount vo = new PedidoRecordCount();
			vo.setCount(rs.getInt(1));
			return vo;
		}catch (SQLException sqle) {
			throw new PedidoRecordCountException(sqle);
		}
		catch (Exception e) {
			throw new PedidoRecordCountException(e);
		}
	}
}
