/**
*					--DAO-Generator--
*
* Java code generated by the DAO-Generator developed by akcess(www.akcess.in)
* Date of code generation: Sun Jun 27 03:16:03 ART 2010
*
* DAO-Generator Version 1.2
*
*/

package metalsoft.datos.dao;
import java.math.*;
import java.sql.*;
import java.net.URL;
import java.util.*;
import metalsoft.datos.exception.*;
import metalsoft.datos.dbobject.*;
import metalsoft.datos.idao.*;


/**
* 
* Implementation of EstadopedidoDAO interface 
* 
*/


public class EstadopedidoDAOImpl implements EstadopedidoDAO
{


/**
* Method deletes a record from table ESTADOPEDIDO
* @param EstadopedidoPK estadopedidopk
* @param  Connection  con
* @return  int
*
*/


	public int delete(EstadopedidoPKDB estadopedidopk , Connection con)throws EstadopedidoException{
		PreparedStatement ps = null;
		try {
			ps = con.prepareStatement("delete from  ESTADOPEDIDO where idestado = ?");
			ps.setLong(1, estadopedidopk.getIdestado());
			return(ps.executeUpdate());
		}catch(SQLException sqle) {throw new EstadopedidoException(sqle);}
		catch(Exception e) {throw new EstadopedidoException(e);}
	}



/**
* This method updates a record in table ESTADOPEDIDO
* @param EstadopedidoPK
* @param Estadopedido
* @param  Connection con
* @return   int
*/

	public int update(EstadopedidoPKDB estadopedidopk, EstadopedidoDB estadopedido, Connection con)throws EstadopedidoException{
		PreparedStatement ps = null;
		try
		{
			ps = con.prepareStatement("update ESTADOPEDIDO set NOMBRE = ? , DESCRIPCION = ?  where idestado = ?");
				ps.setString(1,estadopedido.getNombre());
				ps.setString(2,estadopedido.getDescripcion());
				ps.setLong(3,estadopedidopk.getIdestado());

				return(ps.executeUpdate());
		}catch(SQLException sqle){throw new EstadopedidoException(sqle);}
		catch(Exception e){throw new EstadopedidoException(e);}
	}

/**
* This method inserts data in table ESTADOPEDIDO
*
* @param Estadopedido estadopedido
* @param   Connection con
* @return  EstadopedidoPK
*/

	public int insert(EstadopedidoDB estadopedido ,Connection con)throws EstadopedidoException {

		PreparedStatement ps = null;
		try
		{
			ps = con.prepareStatement("insert into ESTADOPEDIDO( NOMBRE, DESCRIPCION) values (?, ?)");
				ps.setString(1,estadopedido.getNombre());
				ps.setString(2,estadopedido.getDescripcion());

				return(ps.executeUpdate());
		}catch(SQLException sqle){throw new EstadopedidoException(sqle);}
		catch(Exception e){throw new EstadopedidoException(e);}
	}

/**
* 
* Returns a row from the estadopedido table for the primary key passed as parameter.
* 
*/

	public EstadopedidoDB findByPrimaryKey(long idestado, Connection con) throws EstadopedidoException{
		PreparedStatement stmt = null;
		ResultSet rs = null;
		try {
	  		final String SQLSTATEMENT = "Select idestado, nombre, descripcion from estadopedido where idestado = ?";
	  		stmt=con.prepareStatement(SQLSTATEMENT);
	  		stmt.setLong(1, idestado);
	  		rs = stmt.executeQuery();
	  		return fetchSingleResult(rs);
	  	}
	  	catch (SQLException sqle) {
	  		throw new EstadopedidoException(sqle);
	  	}
	    catch(Exception e){throw new EstadopedidoException(e);}
	  	 finally {}
	}

/**
* 
* Returns a row from the estadopedido table for the primary key object passed as parameter.
* 
* @param  EstadopedidoPK estadopedidopk
* @param Connection con
* @return  Estadopedido
*/

	public EstadopedidoDB findByPrimaryKey(EstadopedidoPKDB estadopedidopk, Connection con) throws EstadopedidoException{
		return findByPrimaryKey(estadopedidopk.getIdestado(), con);
	}

/**
*
* Returns all rows from estadopedido table where IDESTADO= idestado
*
* @param   long  idestado
* @param   Connection con
* @return  Estadopedido[]
*/

	public EstadopedidoDB[] findByIdestado(long idestado, Connection con) throws EstadopedidoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idestado, nombre, descripcion from estadopedido where idestado = ? order by idestado";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setLong( 1, idestado );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new EstadopedidoException(sqle);
			}
			catch(Exception e){
					throw new EstadopedidoException(e);
			}
			finally{}
	}

/**
*
* Returns all rows from estadopedido table where NOMBRE= nombre
*
* @param   String  nombre
* @param   Connection con
* @return  Estadopedido[]
*/

	public EstadopedidoDB[] findByNombre(String nombre, Connection con) throws EstadopedidoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idestado, nombre, descripcion from estadopedido where nombre = ? order by nombre";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setString( 1, nombre );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new EstadopedidoException(sqle);
			}
			catch(Exception e){
					throw new EstadopedidoException(e);
			}
			finally{}
	}

/**
*
* Returns all rows from estadopedido table where DESCRIPCION= descripcion
*
* @param   String  descripcion
* @param   Connection con
* @return  Estadopedido[]
*/

	public EstadopedidoDB[] findByDescripcion(String descripcion, Connection con) throws EstadopedidoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idestado, nombre, descripcion from estadopedido where descripcion = ? order by descripcion";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setString( 1, descripcion );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new EstadopedidoException(sqle);
			}
			catch(Exception e){
					throw new EstadopedidoException(e);
			}
			finally{}
	}

/**
* Returns all rows from estadopedido table 
*
* @param Connection con
* @return  Estadopedido[]
*
*/

	public EstadopedidoDB[] findAll( Connection con) throws EstadopedidoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select idestado, nombre, descripcion from estadopedido"+
                                              " ORDER BY nombre";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new EstadopedidoException(sqle);
			}
			catch(Exception e){
					throw new EstadopedidoException(e);
			}
			finally{}
	}

/**
* Returns rows from estadopedido table by executing the passed sql statement
* after setting the passed values in Object[]
*
* @param String selectStatement
* @param Object[] sqlParams
* @param Connection con
* @return  Estadopedido[]
*
*/

	public EstadopedidoDB[] findExecutingUserSelect(String selectStatement, Object[] sqlParams, Connection con) throws EstadopedidoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			final String SQL_STATEMENT = selectStatement;
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					for (int i=0; i<sqlParams.length; i++ ) {
							stmt.setObject( i+1, sqlParams[i] );
					}
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new EstadopedidoException(sqle);
			}
			catch(Exception e){
					throw new EstadopedidoException(e);
			}
			finally{}
	}

/**
* Returns rows from estadopedido table by executing the select all fields statement
* after setting the passed where clause and values in Object[]
*
* @param String whereClause
* @param Object[] sqlParams
* @param Connection con
* @return  Estadopedido[]
*
*/

	public EstadopedidoDB[] findExecutingUserWhere(String whereClause, Object[] sqlParams, Connection con) throws EstadopedidoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_SELECT ="Select idestado, nombre, descripcion from estadopedido";
			final String SQL_STATEMENT =SQL_SELECT + " where " + whereClause;
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					for (int i=0; i<sqlParams.length; i++ ) {
							stmt.setObject( i+1, sqlParams[i] );
					}
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new EstadopedidoException(sqle);
			}
			catch(Exception e){
					throw new EstadopedidoException(e);
			}
			finally{}
	}

/**
*
* Populates a Data Transfer Object by fetching single record from resultSet 
*
* @param ResultSet rs
* @return  Estadopedido
*
*/

	protected EstadopedidoDB fetchSingleResult(ResultSet rs) throws SQLException
	{
			if (rs.next()) {
					EstadopedidoDB dto = new EstadopedidoDB();
					populateVO( dto, rs);
				return dto;
			} else {
				return null;
			}
	}

/**
* 
* Populates a Data Transfer Object by fetching data from  ResultSet
* 
* @param Estadopedido dto
* @param   ResultSet rs
* @return  void
*/

	protected void populateVO(EstadopedidoDB dto, ResultSet rs) throws SQLException
	{
		 dto.setIdestado(rs.getLong("idestado"));
		 dto.setNombre(rs.getString("nombre"));
		 dto.setDescripcion(rs.getString("descripcion"));
	}

/**
* 
* Returns an array of Value Objects by fetching data from resultSet
* 
* @param   ResultSet rs
* @return  Estadopedido[]
*/

	protected EstadopedidoDB[]  fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			EstadopedidoDB dto = new EstadopedidoDB();
			populateVO( dto, rs);
			resultList.add(dto);
		}
		EstadopedidoDB ret[] = new EstadopedidoDB[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}
}
