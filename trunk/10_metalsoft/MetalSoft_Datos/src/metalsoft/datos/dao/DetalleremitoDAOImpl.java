/**
*					--DAO-Generator--
*
* Java code generated by the DAO-Generator developed by akcess(www.akcess.in)
* Date of code generation: Tue Jun 22 00:02:08 ART 2010
*
* DAO-Generator Version 1.2
*
*/

package metalsoft.datos.dao;
import java.math.*;
import java.sql.*;
import java.net.URL;
import java.util.*;
import metalsoft.datos.exception.*;
import metalsoft.datos.dbobject.*;
import metalsoft.datos.idao.*;


/**
* 
* Implementation of DetalleremitoDAO interface 
* 
*/


public class DetalleremitoDAOImpl implements DetalleremitoDAO
{


/**
* Method deletes a record from table DETALLEREMITO
* @param DetalleremitoPK detalleremitopk
* @param  Connection  con
* @return  int
*
*/


	public int delete(DetalleremitoPK detalleremitopk , Connection con)throws DetalleremitoException{
		PreparedStatement ps = null;
		try {
			ps = con.prepareStatement("delete from  DETALLEREMITO where iddetalle = ? AND idremito = ?");
			ps.setLong(1, detalleremitopk.getIddetalle());
			ps.setLong(2, detalleremitopk.getIdremito());
			return(ps.executeUpdate());
		}catch(SQLException sqle) {throw new DetalleremitoException(sqle);}
		catch(Exception e) {throw new DetalleremitoException(e);}
	}



/**
* This method updates a record in table DETALLEREMITO
* @param DetalleremitoPK
* @param Detalleremito
* @param  Connection con
* @return   int
*/

	public int update(DetalleremitoPK detalleremitopk, Detalleremito detalleremito, Connection con)throws DetalleremitoException{
		PreparedStatement ps = null;
		try
		{
			ps = con.prepareStatement("update DETALLEREMITO set CANTIDAD = ? , DESCRIPCION = ? , PRODUCTO = ?  where iddetalle = ? AND idremito = ?");
				ps.setInt(1,detalleremito.getCantidad());
				ps.setString(2,detalleremito.getDescripcion());
				ps.setLong(3,detalleremito.getProducto());
				ps.setLong(4,detalleremitopk.getIddetalle());
				ps.setLong(5,detalleremitopk.getIdremito());

				return(ps.executeUpdate());
		}catch(SQLException sqle){throw new DetalleremitoException(sqle);}
		catch(Exception e){throw new DetalleremitoException(e);}
	}

/**
* This method inserts data in table DETALLEREMITO
*
* @param Detalleremito detalleremito
* @param   Connection con
* @return  DetalleremitoPK
*/

	public int insert(Detalleremito detalleremito ,Connection con)throws DetalleremitoException {

		PreparedStatement ps = null;
		try
		{
			ps = con.prepareStatement("insert into DETALLEREMITO( IDDETALLE, IDREMITO, CANTIDAD, DESCRIPCION, PRODUCTO) values (?, ?, ?, ?, ?)");
				ps.setLong(1,detalleremito.getIddetalle());
				ps.setLong(2,detalleremito.getIdremito());
				ps.setInt(3,detalleremito.getCantidad());
				ps.setString(4,detalleremito.getDescripcion());
				ps.setLong(5,detalleremito.getProducto());

				return(ps.executeUpdate());
		}catch(SQLException sqle){throw new DetalleremitoException(sqle);}
		catch(Exception e){throw new DetalleremitoException(e);}
	}

/**
* 
* Returns a row from the detalleremito table for the primary key passed as parameter.
* 
*/

	public Detalleremito findByPrimaryKey(long iddetalle, long idremito, Connection con) throws DetalleremitoException{
		PreparedStatement stmt = null;
		ResultSet rs = null;
		try {
	  		final String SQLSTATEMENT = "Select iddetalle, idremito, cantidad, descripcion, producto from detalleremito where iddetalle = ? AND idremito = ?";
	  		stmt=con.prepareStatement(SQLSTATEMENT);
	  		stmt.setLong(1, iddetalle);
	  		stmt.setLong(2, idremito);
	  		rs = stmt.executeQuery();
	  		return fetchSingleResult(rs);
	  	}
	  	catch (SQLException sqle) {
	  		throw new DetalleremitoException(sqle);
	  	}
	    catch(Exception e){throw new DetalleremitoException(e);}
	  	 finally {}
	}

/**
* 
* Returns a row from the detalleremito table for the primary key object passed as parameter.
* 
* @param  DetalleremitoPK detalleremitopk
* @param Connection con
* @return  Detalleremito
*/

	public Detalleremito findByPrimaryKey(DetalleremitoPK detalleremitopk, Connection con) throws DetalleremitoException{
		return findByPrimaryKey(detalleremitopk.getIddetalle(), detalleremitopk.getIdremito(), con);
	}

/**
*
* Returns all rows from detalleremito table where IDDETALLE= iddetalle
*
* @param   long  iddetalle
* @param   Connection con
* @return  Detalleremito[]
*/

	public Detalleremito[] findByIddetalle(long iddetalle, Connection con) throws DetalleremitoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select iddetalle, idremito, cantidad, descripcion, producto from detalleremito where iddetalle = ? order by iddetalle";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setLong( 1, iddetalle );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new DetalleremitoException(sqle);
			}
			catch(Exception e){
					throw new DetalleremitoException(e);
			}
			finally{}
	}

/**
*
* Returns all rows from detalleremito table where IDREMITO= idremito
*
* @param   long  idremito
* @param   Connection con
* @return  Detalleremito[]
*/

	public Detalleremito[] findByIdremito(long idremito, Connection con) throws DetalleremitoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select iddetalle, idremito, cantidad, descripcion, producto from detalleremito where idremito = ? order by idremito";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setLong( 1, idremito );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new DetalleremitoException(sqle);
			}
			catch(Exception e){
					throw new DetalleremitoException(e);
			}
			finally{}
	}

/**
*
* Returns all rows from detalleremito table where CANTIDAD= cantidad
*
* @param   int  cantidad
* @param   Connection con
* @return  Detalleremito[]
*/

	public Detalleremito[] findByCantidad(int cantidad, Connection con) throws DetalleremitoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select iddetalle, idremito, cantidad, descripcion, producto from detalleremito where cantidad = ? order by cantidad";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setInt( 1, cantidad );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new DetalleremitoException(sqle);
			}
			catch(Exception e){
					throw new DetalleremitoException(e);
			}
			finally{}
	}

/**
*
* Returns all rows from detalleremito table where DESCRIPCION= descripcion
*
* @param   String  descripcion
* @param   Connection con
* @return  Detalleremito[]
*/

	public Detalleremito[] findByDescripcion(String descripcion, Connection con) throws DetalleremitoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select iddetalle, idremito, cantidad, descripcion, producto from detalleremito where descripcion = ? order by descripcion";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setString( 1, descripcion );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new DetalleremitoException(sqle);
			}
			catch(Exception e){
					throw new DetalleremitoException(e);
			}
			finally{}
	}

/**
*
* Returns all rows from detalleremito table where PRODUCTO= producto
*
* @param   long  producto
* @param   Connection con
* @return  Detalleremito[]
*/

	public Detalleremito[] findByProducto(long producto, Connection con) throws DetalleremitoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select iddetalle, idremito, cantidad, descripcion, producto from detalleremito where producto = ? order by producto";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					stmt.setLong( 1, producto );
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new DetalleremitoException(sqle);
			}
			catch(Exception e){
					throw new DetalleremitoException(e);
			}
			finally{}
	}

/**
* Returns all rows from detalleremito table 
*
* @param Connection con
* @return  Detalleremito[]
*
*/

	public Detalleremito[] findAll( Connection con) throws DetalleremitoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_STATEMENT ="Select iddetalle, idremito, cantidad, descripcion, producto from detalleremito";
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new DetalleremitoException(sqle);
			}
			catch(Exception e){
					throw new DetalleremitoException(e);
			}
			finally{}
	}

/**
* Returns rows from detalleremito table by executing the passed sql statement
* after setting the passed values in Object[]
*
* @param String selectStatement
* @param Object[] sqlParams
* @param Connection con
* @return  Detalleremito[]
*
*/

	public Detalleremito[] findExecutingUserSelect(String selectStatement, Object[] sqlParams, Connection con) throws DetalleremitoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			final String SQL_STATEMENT = selectStatement;
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					for (int i=0; i<sqlParams.length; i++ ) {
							stmt.setObject( i+1, sqlParams[i] );
					}
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new DetalleremitoException(sqle);
			}
			catch(Exception e){
					throw new DetalleremitoException(e);
			}
			finally{}
	}

/**
* Returns rows from detalleremito table by executing the select all fields statement
* after setting the passed where clause and values in Object[]
*
* @param String whereClause
* @param Object[] sqlParams
* @param Connection con
* @return  Detalleremito[]
*
*/

	public Detalleremito[] findExecutingUserWhere(String whereClause, Object[] sqlParams, Connection con) throws DetalleremitoException{
			PreparedStatement stmt = null;
			ResultSet rs = null;
			String SQL_SELECT ="Select iddetalle, idremito, cantidad, descripcion, producto from detalleremito";
			final String SQL_STATEMENT =SQL_SELECT + " where " + whereClause;
			try {
					stmt = con.prepareStatement(SQL_STATEMENT);
					for (int i=0; i<sqlParams.length; i++ ) {
							stmt.setObject( i+1, sqlParams[i] );
					}
					rs = stmt.executeQuery();
					return fetchMultiResults(rs);
			}catch(SQLException sqle){
					throw new DetalleremitoException(sqle);
			}
			catch(Exception e){
					throw new DetalleremitoException(e);
			}
			finally{}
	}

/**
*
* Populates a Data Transfer Object by fetching single record from resultSet 
*
* @param ResultSet rs
* @return  Detalleremito
*
*/

	protected Detalleremito fetchSingleResult(ResultSet rs) throws SQLException
	{
			if (rs.next()) {
					Detalleremito dto = new Detalleremito();
					populateVO( dto, rs);
				return dto;
			} else {
				return null;
			}
	}

/**
* 
* Populates a Data Transfer Object by fetching data from  ResultSet
* 
* @param Detalleremito dto
* @param   ResultSet rs
* @return  void
*/

	protected void populateVO(Detalleremito dto, ResultSet rs) throws SQLException
	{
		 dto.setIddetalle(rs.getLong("iddetalle"));
		 dto.setIdremito(rs.getLong("idremito"));
		 dto.setCantidad(rs.getInt("cantidad"));
		 dto.setDescripcion(rs.getString("descripcion"));
		 dto.setProducto(rs.getLong("producto"));
	}

/**
* 
* Returns an array of Value Objects by fetching data from resultSet
* 
* @param   ResultSet rs
* @return  Detalleremito[]
*/

	protected Detalleremito[]  fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Detalleremito dto = new Detalleremito();
			populateVO( dto, rs);
			resultList.add(dto);
		}
		Detalleremito ret[] = new Detalleremito[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}
}
